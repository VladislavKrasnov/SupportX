[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -U setuptools pip", "pip install -r requirements.txt"]

[start]
cmd = "yoyo apply --database $DATABASE_URL -b && python -m bot"
